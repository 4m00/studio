import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function InitiativesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Инициативы</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Эта страница находится в разработке.</p>
      </CardContent>
    </Card>
  );
}
